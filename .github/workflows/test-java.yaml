name: Test java (remove after use)
on: [push]
jobs:
  test-java:
    runs-on: ubuntu-latest
    steps:
      # - uses: actions/checkout@v2

      # - uses: actions/checkout@v2
      #   with:
      #     repository: kepler16/actions
      #     ref: e05c36f8895210251d3aac9307adfed845764749
      #     token: ${{secrets.GITHUB_TOKEN}}
      #     path: .github/actions

      # - uses: ./.github/actions/setup-java
      #   with:
      #     token: ${{secrets.GITHUB_TOKEN}}

      # - uses: ./.github/actions/setup-clojure
      # - uses: ./.github/actions/setup-babashka
      # - uses: ./.github/actions/setup-node

      # - run: java --version
      # - run: clojure --version
      # - run: bb --version
      # - run: node --version
      # - run: npm --version
      # - run: npm install

      - id: first
        name: First
        run: |
          VAR=$(test -e .VAR && cat .VAR || echo "NOV-VAR")
          echo " orig var $VAR"
          echo '::set-output name=var::$VAR'

      - id: second
        name: SEcond
        run: echo "steps var ${{ steps.first.outputs.var }}"
